//  CÓDIGO NÃO EXECUTÁVEL

// Procedural
processamento(valor1, valor2, valor3)

  // OO
  objeto = {
    valor1,
    valor2,
    valor3,
    processamento() {
      // ...
    }
  }

  objeto.processamento() // Foco passou a ser o objeto

  // Principios importantes:
  // 1. Abstração : Pegar um objeto da vida real e traduzir para o seu código(sistema)
  // 2. Encapsulamento: Detalhes de implementação escondidos, e você mostrar para o cliente(usuário do sistema), a interface simples para interagir com determinado objeto
  // 3. Herança: (prototype) priorize a composição em vez da herança
  // 4. Polimorfismo: